# Fixture Tracker

Run:

    pip install -r requirements.txt
    uvicorn app:app --reload

Open http://127.0.0.1:8000

Current build includes the ShopCore-inspired shell and Settings > Folder Paths. The Fixture Pool path is prefilled from the USB drive; Machine Jobs is intentionally blank.
